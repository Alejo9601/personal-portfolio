---
const { side } = Astro.props;
const gradient = side == "left" ? "bg-gradient-to-r" : "bg-gradient-to-l";
const position = side == "left" ? "left-0" : "right-0";
---

<div
   class=`absolute ${position} ${gradient} h-full w-14 md:w-20 lg:w-28 dark:from-slate-900 from-slate-200 to-transparent z-[1]`
>
</div>
